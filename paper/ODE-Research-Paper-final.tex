\documentclass[11pt,a4paper]{article}
\usepackage[utf8]{inputenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage{amsthm}
\usepackage{graphicx}
\usepackage{geometry}
\usepackage{hyperref}
\usepackage{listings}
\usepackage{xcolor}

% Custom geometry
\geometry{a4paper, margin=1in}

% Theorem environments
\newtheorem{theorem}{Theorem}
\theoremstyle{definition}
\newtheorem{definition}{Definition}

% SymPy code highlighting style
\lstset{
    language=Python,
    basicstyle=\ttfamily\small,
    breaklines=true,
    keywordstyle=\color{blue},
    stringstyle=\color{red},
    commentstyle=\color{green!60!black},
    numbers=left,
    numberstyle=\tiny\color{gray},
    frame=single,
    backgroundcolor=\color{gray!5}
}

% Author metadata
\title{An Inverse Riccati Construction of Second-Order Linear ODEs from Degree-2 Algebraic Extensions}
\author{Kevin D. Potter\footnote{Saint Leo Alumni; Potter's Quill Publishing; development and operation of the Brodie Foxworth AI research agent.}}
\date{July 5, 2026}

\begin{document}

\maketitle

\begin{abstract}
We present a systematic construction of a family of second-order linear ODEs of the form \(y'' + r(z)y = 0\) whose solutions are Liouvillian and arise from a logarithmic derivative \(s(z) \in \mathbb{C}(z, \sqrt{R})\) of algebraic degree exactly 2 over \(\mathbb{C}(z)\). The construction places two poles at \(z=0\) and \(z=1\) with equal residues \(1/4\), yielding the potential \(r(z) = \frac{3}{16z^2(z-1)^2}\) and solutions expressible in terms of radicals and inverse hyperbolic functions. We verify the Riccati equation by direct computation, prove that the logarithmic derivative has algebraic degree 2 via its minimal polynomial, and establish linear independence of the two fundamental solutions via their nonconstant ratio and constant Wronskian. An outline of an \(n\)-pole generalization is included. We have not found this exact inverse two-pole construction in the standard references consulted.
\end{abstract}

\section{Introduction}

The Kovacic algorithm~\cite{kovacic1986} provides a systematic method for determining whether a second-order linear ODE of the form
\begin{equation}
y'' + r(z)y = 0
\label{eq:ODE}
\end{equation}
has Liouvillian solutions, i.e., solutions expressible in terms of exponentials, logarithms, and algebraic functions. The algorithm operates by examining the poles of the coefficient function \(r(z)\) and classifying the local behavior at each singular point into one of four cases, each of which constrains the possible algebraic degree of the logarithmic derivative of a solution.

A Liouvillian solution of~\eqref{eq:ODE} can be written as \(y_1 = \exp(\int s(z)\,dz)\), where \(s(z) \in \mathbb{C}(z, \sqrt{R})\) satisfies the Riccati equation
\begin{equation}
s' + s^2 + r(z) = 0.
\end{equation}
The algebraic degree of \(s(z)\) over \(\mathbb{C}(z)\) determines the complexity of the solution. Kovacic's algorithm searches for \(s(z)\) of degree 1, 2, 4, or 8 over \(\mathbb{C}(z)\). In this paper we focus on degree-2 solutions, where \(s(z) \in \mathbb{C}(z, \sqrt{R})\) for some square-free \(R \in \mathbb{C}(z)\) with \(\sqrt{R} \notin \mathbb{C}(z)\).

We present a construction that starts from the desired algebraic structure---a quadratic extension \(\mathbb{C}(z, \sqrt{R})/\mathbb{C}(z)\)---and derives the ODE coefficient \(r(z)\) from the Riccati equation. This is the reverse of the usual approach, which starts from \(r(z)\) and attempts to find \(s(z)\). Our construction naturally produces ODEs with two poles, in contrast to many of the single-pole or single-point examples we encountered in the references consulted within Kovacic's framework.

\section{The Two-Pole Ansatz}

We seek a logarithmic derivative of the form
\begin{equation}
s(z) = A(z) + B(z)\sqrt{R(z)}, \quad A, B, R \in \mathbb{C}(z), \quad \sqrt{R} \notin \mathbb{C}(z),
\end{equation}
where \(s(z)\) lies in the quadratic extension \(\mathbb{C}(z, \sqrt{R})\) of \(\mathbb{C}(z)\). The Riccati equation implies \(r(z) = -(s' + s^2)\).

We place two poles at \(z=0\) and \(z=1\) with equal residues \(1/4\). This gives
\begin{align}
A(z) &= \frac{2z-1}{4z(z-1)} = \frac{1}{4z} + \frac{1}{4(z-1)}, \label{eq:defA} \\
R(z) &= \frac{1}{z(z-1)}, \label{eq:defR} \\
B &= c = \frac{1}{2}. \label{eq:defB}
\end{align}
The choice \(B=c=1/2\) is the simplest case of the general framework where \(B=cf(z)\) for some \(f(z) \in \mathbb{C}(z)\). The irrational cancellation condition \(R'/2 + 2AR = 0\) is independent of \(B\). For \(f(z)=1\), this cancellation is an algebraic identity, verified by logarithmic differentiation of \(R = \prod_i (z-z_i)^{-n_i}\). For general \(f(z)\), additional constraints are needed to ensure \(r(z) \in \mathbb{C}(z)\); this case is left for future work. The resulting potential depends on \(B^2\); the choice \(B=1/2\) gives the simple potential used in the two-pole example below. For the simplest subfamily \(f(z)=1\), the constraint gives \(B=c\), a constant. This yields the two branches
\begin{equation}
s_{\pm}(z) = \frac{2z-1}{4z(z-1)} \pm \frac{1}{2\sqrt{z(z-1)}}. \label{eq:branchesS}
\end{equation}
The two poles of \(A\) are located at \(z=0\) and \(z=1\), each with residue \(1/4\); the branch cut for \(\sqrt{z(z-1)}\) is taken along $[0, 1]$.

\subsection{Field-Extension Tower}
\[
\begin{array}{c}
\mathbb{C}(z, \sqrt{R}) \quad \text{[degree 2]} \\
\uparrow \scriptstyle{\sqrt{R} \notin \mathbb{C}(z)} \\
\mathbb{C}(z) \quad \text{[rational functions]}
\end{array}
\]
\begin{center}
    \textit{Quadratic extension for \(s(z)\).}
\end{center}

\section{The Construction}

\subsection{Choosing the Poles and Computing A, B, R, s}
We choose poles at \(z_1 = 0\) and \(z_2 = 1\) with residues \(a_1 = a_2 = 1/4\) (corresponding to \(n_1 = n_2 = 1\), both odd). The partial-fraction decomposition of \(A\) is
\begin{equation*}
A(z) = \frac{2z-1}{4z(z-1)} = \frac{1}{4z} + \frac{1}{4(z-1)}.
\end{equation*}
The radicand \(R\) encodes the irrational part of the logarithmic derivative:
\begin{equation*}
R(z) = \frac{1}{z(z-1)}.
\end{equation*}
With \(B=c=1/2\) (the simplest subfamily, \(f(z)=1\)), the two branches of the logarithmic derivative are
\begin{equation*}
s_{\pm}(z) = \frac{2z-1}{4z(z-1)} \pm \frac{1}{2\sqrt{z(z-1)}}.
\end{equation*}

\section{The ODE and Its Solutions}

\subsection{The Potential \(r(z)\)}
From the Riccati equation, \(r(z) = -(s' + s^2)\). We first compute the rational part \(A' + A^2 + B^2R\). The irrational part is shown below to vanish for this choice of \(A\) and \(R\):
\begin{align}
A' &= -\frac{1}{4z^2} - \frac{1}{4(z-1)^2}, \\
A^2 &= \frac{1}{16z^2} + \frac{1}{8z(z-1)} + \frac{1}{16(z-1)^2}, \\
B^2R &= \frac{1}{4z(z-1)}.
\end{align}
Summing these three contributions,
\begin{equation}
A' + A^2 + B^2R = -\frac{3}{16z^2} - \frac{3}{16(z-1)^2} + \frac{3}{8z(z-1)} = -\frac{3}{16z^2(z-1)^2}.
\end{equation}
Therefore
\begin{equation}
r(z) = -(A' + A^2 + B^2R) = \frac{3}{16z^2(z-1)^2}. \label{eq:potential}
\end{equation}
The irrational parts cancel because \(R'/2 + 2AR = 0\) for our specific \(A\) and \(R\), as verified explicitly in Theorem 1. Thus both branches yield the same \(r(z) \in \mathbb{C}(z)\).

\subsection{Integrating \(s\) to Obtain \(y_1\)}
The first solution is
\begin{equation}
y_1 = \exp\left(\int s_{+}\,dz\right).
\end{equation}
We split the integral:
\begin{equation}
\int s_{+}\,dz = \int \frac{2z-1}{4z(z-1)}\,dz + \int \frac{1}{2\sqrt{z(z-1)}}\,dz.
\end{equation}
The first integral is elementary, and the second is expressed using \(\text{arcosh}\):
\begin{equation}
\int s_{+}\,dz = \frac{1}{4}\log(z(z-1)) + \frac{1}{2}\text{arcosh}(2z-1).
\end{equation}
Using the complex identity \(\text{arcosh}(2z-1) = \log(2z-1 + 2\sqrt{z(z-1)})\), valid after choosing compatible branches, we obtain \(\int s_{+}\,dz = \frac{1}{4}\log(z(z-1)) + \frac{1}{2}\log(2z-1 + 2\sqrt{z(z-1)})\). Exponentiating gives
\begin{equation}
y_1 = [z(z-1)]^{1/4}[2z-1 + 2\sqrt{z(z-1)}]^{1/2}. \label{eq:sol1}
\end{equation}

\subsection{The Second Solution \(y_2\) from the Conjugate}
The second solution arises from the conjugate logarithmic derivative
\begin{equation}
s_2 = A - B\sqrt{R} \label{eq:logder2}
\end{equation}
obtained by applying the nontrivial automorphism of the quadratic extension \(\mathbb{C}(z, \sqrt{R})/\mathbb{C}(z)\) which sends \(\sqrt{R} \mapsto -\sqrt{R}\). This yields
\begin{equation}
y_2 = [z(z-1)]^{1/4}[2z-1 - 2\sqrt{z(z-1)}]^{1/2}. \label{eq:sol2}
\end{equation}
The automorphism preserves the Riccati equation because \(r(z) \in \mathbb{C}(z)\) is fixed, so \(s_2\) is also a root and \(y_2 = \exp(\int s_2\,dz)\) is also a solution.

\section{Proofs}

\begin{theorem}[Riccati Verification]
Let \(A(z) = \frac{2z-1}{4z(z-1)}\), \(B = \frac{1}{2}\), \(R(z) = \frac{1}{z(z-1)}\), and \(s_{\pm} = A \pm B\sqrt{R}\). Define \(r(z) = -(A' + A^2 + B^2R)\). Then
\begin{equation*}
r(z) = \frac{3}{16z^2(z-1)^2}
\end{equation*}
and both \(s_{+}\) and \(s_{-}\) satisfy the Riccati equation \(s' + s^2 + r(z) = 0\).
\end{theorem}

\begin{proof}
The rational computation is given in \eqref{eq:potential}. It remains to verify that the irrational part vanishes. For \(s_{+} = A + B\sqrt{R}\),
\begin{equation*}
s_{+}' + s_{+}^2 = A' + A^2 + B^2R + \left(\frac{BR'}{2\sqrt{R}} + 2AB\sqrt{R}\right).
\end{equation*}
The irrational part is \(\frac{B}{\sqrt{R}}\left(\frac{R'}{2} + 2AR\right)\). For our specific \(A\) and \(R\),
\begin{equation*}
\frac{R'}{2} + 2AR = -\frac{2z-1}{2z^2(z-1)^2} + \frac{2(2z-1)}{4z(z-1)}\cdot\frac{1}{z(z-1)} = -\frac{2z-1}{2z^2(z-1)^2} + \frac{2z-1}{2z^2(z-1)^2} = 0.
\end{equation*}
Thus the irrational part vanishes, and \(s_{+}' + s_{+}^2 = A' + A^2 + B^2R = -r(z)\). The case \(s_{-} = A - B\sqrt{R}\) is identical with \(B\) replaced by \(-B\); since the irrational term is proportional to \(B\) and equals zero, both branches satisfy the Riccati equation.
\end{proof}

\begin{theorem}[Algebraic Degree 2]
The logarithmic derivative \(s = A + B\sqrt{R}\) has algebraic degree exactly 2 over \(\mathbb{C}(z)\); that is, \(s \notin \mathbb{C}(z)\) and \(s\) satisfies a degree-2 minimal polynomial over \(\mathbb{C}(z)\).
\end{theorem}

\begin{proof}
Since \(s = A + B\sqrt{R}\) with \(B \neq 0\), the minimal polynomial is obtained by eliminating \(\sqrt{R}\): \((s-A)^2 = B^2R\), or
\begin{equation}
s^2 - 2As + (A^2 - B^2R) = 0. \label{eq:minpoly_general}
\end{equation}
Substituting \(A = (2z-1)/(4z(z-1))\), \(B = 1/2\), and \(R = 1/(z(z-1))\) gives:
\begin{equation}
s^2 - \frac{2z-1}{2z(z-1)}s + \frac{1}{16z^2(z-1)^2} = 0. \label{eq:minpoly_specific}
\end{equation}
Here \(R = 1/(z(z-1))\) is not a square in \(\mathbb{C}(z)\), since it has simple poles at \(z=0\) and \(z=1\), while a square in \(\mathbb{C}(z)\) can only have even-order poles. Thus \(\sqrt{R} \notin \mathbb{C}(z)\); since \(B \neq 0\), \(s \notin \mathbb{C}(z)\). The polynomial~\eqref{eq:minpoly_specific} has degree 2 in \(s\) with coefficients in \(\mathbb{C}(z)\). Since \(s \notin \mathbb{C}(z)\), it is irreducible over \(\mathbb{C}(z)\) and hence is the minimal polynomial.
\end{proof}

\begin{theorem}[Linear Independence]
The solutions \(y_1\) and \(y_2\) are linearly independent over \(\mathbb{C}\).
\end{theorem}

\begin{proof}
Set \(u = 2z-1 + 2\sqrt{z(z-1)}\) and \(v = 2z-1 - 2\sqrt{z(z-1)}\). The identity \((2z-1)^2 - 4z(z-1) = 1\) gives \(uv = 1\). On any branch where \(y_1\) and \(y_2\) are defined, their ratio is
\begin{equation*}
\frac{y_1}{y_2} = \left(\frac{u}{v}\right)^{1/2}.
\end{equation*}
Since \(v = 1/u\), this ratio is \(\pm u\), depending on branch choices. But \(u = 2z-1 + 2\sqrt{z(z-1)}\) is a nonconstant algebraic function. Therefore \(y_1/y_2\) is not constant on any open domain where the branches are defined, and \(y_1, y_2\) are linearly independent.

Additionally, by Abel's identity, the Wronskian is constant because the ODE has no \(y'\) term. Direct evaluation gives \(W(y_1, y_2) = \pm 1\), which is constant and nonzero on each branch.
\end{proof}

\section{Computational Verification}

The following SymPy session verifies the key identities computationally. This section is provided for reproducibility and is separate from the formal proofs above.

\begin{lstlisting}[caption={SymPy Verification Session}]
import sympy as sp
z = sp.Symbol('z')

# Define A, B, R
A = (2*z - 1) / (4*z*(z - 1))
B = sp.Rational(1, 2)
R = 1 / (z*(z - 1))

# Define s_+ and s_-
s_plus = A + B * sp.sqrt(R)
s_minus = A - B * sp.sqrt(R)

# Verify r(z) = -(A' + A^2 + B^2*R)
r = -(sp.diff(A, z) + A**2 + B**2*R)
r_simplified = sp.simplify(r)
print("r(z) =", r_simplified)
# Output: 3/(16*z**2*(z-1)**2) -- OK

# Verify Riccati: s' + s^2 + r = 0 for both branches
ric_plus = sp.simplify(sp.diff(s_plus, z) + s_plus**2 + r)
ric_minus = sp.simplify(sp.diff(s_minus, z) + s_minus**2 + r)
print("Riccati s+:", ric_plus) # 0 -- OK
print("Riccati s-:", ric_minus) # 0 -- OK

# Verify minimal polynomial
s = sp.Symbol('s')
minpoly = s**2 - 2*A*s + (A**2 - B**2*R)
print("Minimal polynomial:", sp.simplify(minpoly))
# degree 2 in s -- OK

# Verify key identity: (2z-1)^2 - 4*z*(z-1) = 1
identity = sp.expand((2*z - 1)**2 - 4*z*(z - 1))
print("(2z-1)^2 - 4z(z-1) =", identity) # 1 -- OK

# Verify solutions
y1 = (z*(z-1))**sp.Rational(1,4) * \
     (2*z - 1 + 2*sp.sqrt(z*(z-1)))**sp.Rational(1,2)
y2 = (z*(z-1))**sp.Rational(1,4) * \
     (2*z - 1 - 2*sp.sqrt(z*(z-1)))**sp.Rational(1,2)

# Check ODE: y'' + r*y = 0
ode_check_1 = sp.simplify(sp.diff(y1, z, 2) + r * y1)
ode_check_2 = sp.simplify(sp.diff(y2, z, 2) + r * y2)
print("ODE check y1:", sp.simplify(ode_check_1)) # 0 -- OK
print("ODE check y2:", sp.simplify(ode_check_2)) # 0 -- OK

# Wronskian
W = sp.simplify(y1 * sp.diff(y2, z) - sp.diff(y1, z) * y2)
print("Wronskian:", W)
# Branch-aware simplification gives W = +/- 1 (nonzero) -- OK

# Ratio y1/y2
ratio = sp.simplify(y1 / y2)
print("y1/y2 =", ratio)
# Using uv=1, manual simplification gives y1/y2 = +/- u (nonconstant) -- OK
\end{lstlisting}
All computational checks confirm the theoretical results. The SymPy verification is consistent with the formal proofs in Section 5.

\section{Outline of an \(n\)-Pole Generalization}

The construction extends formally to \(n\) poles at \(z_1, \dots, z_n\) with residues \(a_i = n_i/4\), where each \(n_i\) is odd. The framework is
\begin{align}
A(z) &= \sum_{i=1}^{n} \frac{a_i}{z-z_i}, \quad a_i = \frac{n_i}{4}, \quad n_i \in \{1, 3, 5, \dots\} \\
\exp\left(-2\int A\,dz\right) &= \prod_{i=1}^{n} (z-z_i)^{-2a_i} = \prod_{i=1}^{n} (z-z_i)^{-n_i/2}.
\end{align}
The square root of \(R\) is then
\begin{equation}
\sqrt{R} = \frac{\prod_{i=1}^{n} (z-z_i)^{-n_i/2}}{f(z)}, \quad f(z) \in \mathbb{C}(z).
\end{equation}
The radicand and coefficient \(B\) are
\begin{align}
R &= \frac{\prod_{i=1}^{n} (z-z_i)^{-n_i}}{f(z)^2} \\
B &= c f(z).
\end{align}
The cancellation condition is independent of $B$, but the resulting potential depends on $B^2$. In the simplest subfamily $f(z)=1$, the parametrization $B=cf(z)$ gives $B=c$, a constant. This recovers the two-pole example treated above. For general $f(z)$ the analysis becomes more involved and is left for future work. When the cancellation condition is satisfied, the ODE coefficient is \(r(z) = -(A' + A^2 + B^2R)\).

\section{Comparison with Kovacic's Examples}

Kovacic's original paper~\cite{kovacic1986} and subsequent literature, such as~\cite{vanderput2003, singer1991}, study ODEs with a single irregular singular point or specific pole configurations arising from special-function theory, including Bessel, Airy, and hypergeometric examples. Our construction differs from examples we encountered in the consulted references in the following sense:

\begin{center}
\begin{tabular}{|l|p{6cm}|p{6cm}|}
\hline
\textbf{Feature} & \textbf{Examples encountered in consulted references} & \textbf{This construction} \\
\hline
Pole structure & Single or multi-pole configurations & Two distinct poles at \(z=0\) and \(z=1\) \\
\hline
Explicit \(r(z)\) & Varies, e.g., \(\frac{1}{4z^2}\), \(\frac{-1+3z^2}{4z^2(z^2-1)^2}\) & \(\frac{3}{16z^2(z-1)^2}\) \\
\hline
Algebraic extension & Examples such as \(\mathbb{C}(z, \sqrt{az+b})\) & \(\mathbb{C}(z, \sqrt{1/(z(z-1))})\) \\
\hline
Construction direction & Given \(r(z)\), find \(s(z)\) & Given the structure of \(s(z)\), derive \(r(z)\) \\
\hline
Residue pattern & Various & Equal residues \(1/4\) at each pole \\
\hline
\end{tabular}
\end{center}

In the examples from Kovacic and the references we consulted~\cite{kovacic1986, vanderput2003, singer1991, kovacic1986notes}, we did not find this exact inverse two-pole construction. The construction is natural from the perspective of inverse Riccati problems but appears to be less common in the examples we consulted.

\section{Conclusion}

We have presented a systematic construction of second-order linear ODEs with degree-2 Liouvillian solutions, based on a two-pole ansatz for the logarithmic derivative \(s(z) = A(z) + B(z)\sqrt{R(z)}\). The specific example with poles at \(z=0\) and \(z=1\) yields the ODE \(y'' + \frac{3}{16z^2(z-1)^2}y = 0\) with solutions
\begin{align*}
y_1 &= [z(z-1)]^{1/4}[2z-1 + 2\sqrt{z(z-1)}]^{1/2} \\
y_2 &= [z(z-1)]^{1/4}[2z-1 - 2\sqrt{z(z-1)}]^{1/2}
\end{align}
The construction was verified by: (i) direct computation of \(r(z)\) from \(-(A' + A^2 + B^2R)\); (ii) proof that \(s\) has algebraic degree exactly 2 via its minimal polynomial; (iii) proof of linear independence via the nonconstant ratio \(y_1/y_2\) and constant nonzero Wronskian \(W=\pm 1\); and (iv) supporting SymPy computational checks.

The \(n\)-pole generalization extends the framework to arbitrary numbers of poles with odd residue parameters, with the simplest subfamily \(f(z)=1\) yielding constant \(B=c\). Further exploration of nonconstant \(f(z)\) and the resulting ODE families is a natural direction for future work.

\section*{Author Contributions}
Mark F. Kruelle formulated the initial mathematical question and reviewed the mathematical direction. Kevin D. Potter developed and operated the Brodie Foxworth AI research agent, directed the computational and drafting workflow, reviewed the mathematical claims, and prepared the manuscript. Kevin D. Potter reviewed and approved the final manuscript and accepts responsibility for its content.

\section*{AI Assistance Disclosure}
Brodie Foxworth, an AI scientific research agent built by Kevin D. Potter, to assist with symbolic exploration, draft organization, LaTeX preparation, computational-check workflow support, and editorial review. Brodie Foxworth is not an author. The human authors reviewed the mathematical claims, computations, citations, and conclusions, and accept full responsibility for the manuscript.

\newpage

\bibliographystyle{plain}
\begin{thebibliography}{9}

\bibitem{kovacic1986}
J.~J. Kovacic.
\newblock An algorithm for solving second order linear homogeneous differential equations.
\newblock {\em Journal of Symbolic Computation}, 2(1):3--43, 1986.

\bibitem{vanderput2003}
M.~van~der Put and M.~F. Singer.
\newblock {\em Galois Theory of Linear Differential Equations}.
\newblock Springer, 2003.

\bibitem{singer1991}
M.~F. Singer.
\newblock Liouvillian solutions of linear differential equations with Liouvillian coefficients.
\newblock {\em Journal of Symbolic Computation}, 11:251--273, 1991.

\bibitem{kovacic1986notes}
J.~J. Kovacic.
\newblock An algorithm for solving second order linear homogeneous differential equations---supplementary documentation.
\newblock unpublished notes, 1986.

\bibitem{churchill1985}
R.~C. Churchill and J.~J. Kovacic.
\newblock Cyclic vectors.
\newblock In {\em Differential Galois Theory}, volume 41 of {\em Contemporary Mathematics}. American Mathematical Society, 1985.

\end{thebibliography}

\end{document}
